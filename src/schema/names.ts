// The shape of every name a script gives (targets, cues, scenes, areas), on its own so the browser
// can check a name without loading the whole schema.

export const NAME_PATTERN = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;
